# Azure SQL Database Configuration
# You'll need to create an Azure SQL Database and add these values

# Azure SQL Database connection details
AZURE_SQL_SERVER=your-server-name.database.windows.net
AZURE_SQL_DATABASE=your-database-name
AZURE_SQL_USER=your-username
AZURE_SQL_PASSWORD=your-password

# Optional: If using Azure AD authentication instead of SQL authentication
# AZURE_SQL_AUTHENTICATION=ActiveDirectoryPassword
# AZURE_CLIENT_ID=your-app-client-id
# AZURE_TENANT_ID=your-tenant-id

# Development/Production environment
NODE_ENV=development

# Port configuration (development only)
PORT=3000

# Authentication settings
IS_AUTH_ENABLED=false
